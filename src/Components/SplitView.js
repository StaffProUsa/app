import React from 'react';
import { SView } from 'servisofts-component';

export default () => {
    return <SView col={"xs-12"} row>
        <SView style={{ width: 100, backgroundColor: "#f0f" }}>

        </SView>
        <SView style={{ flex: 1, backgroundColor: "#ff0" }}>

        </SView>
    </SView>
}